{
  "name": "shipit-worker",
  "version": "1.0.0",
  "description": "ShipIt — Cloudflare Worker that bridges Telegram to GitHub Actions",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
